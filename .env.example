# WebSocket server URL (app appends /ws)
TOKENHUB_SERVER=ws://localhost:8080

# ngrok auth token — tunnel works without it but with rate limits
# Get one at https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=
