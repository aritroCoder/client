[project]
name = "tokenhub-client"
version = "0.1.0"
description = "TokenHub exchange client"
requires-python = ">=3.11"
dependencies = ["textual>=1.0.0", "httpx>=0.28.0", "aiohttp>=3.11.0"]

[project.scripts]
tokenhub = "client.app:main"
